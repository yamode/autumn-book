# autumn-book アーキテクチャ決定記録（ADR-0001）：施設HPとブランドポータルの分離

- **ステータス**: Accepted（2026-06-21）
- **関連**: `autumn_book_design.md`（§0・§4.0 のHP方針を本ADRで置換）／`autumn_book_forum_design.md`（掲示板はポータルに帰属＝本ADRと整合）／`autumn_book_portal_sitemap.md`（ポータルのルート全体像・本ADRの URL 設計を具体化）
- **決定者**: ユーザー（hikaru）。本書は合意済みの方針を記録したもの。詳細設計への展開は規約どおり Fable 5 で行う。

---

## 背景（Context）

- 西和賀「山人 -yamado-」(`www.yamado.co.jp`) と 男鹿「山人 -oga-」(`oga.yamado.co.jp`) は、それぞれ**別個の WordPress サイト**として運用中で、各々が独自の予約エンジンを持つ。当面の目的は、この**予約エンジンを autumn-book に置き換える**こと。
- 当初設計（`autumn_book_design.md` v2）は **単一 SvelteKit アプリが全施設HP＋予約＋会員＋ポータルを配信**する一枚岩。施設ごとの演出差分は `YamadoShell` / `OgaShell` / `StandardShell` で出し分け、SvelteKit `reroute` で Host→施設slug を解決する想定だった。
- **摩擦点**：施設ごとに世界観（演出）が異なるため、**HPを足すたびに Svelte シェルを作り込み、予約エンジン本体と同じリポ／デプロイに結合**する。HPの反復速度がエンジンの安定性に縛られ、既存WPの再構築コストも発生する。
- **検討した代替案**：tripla 型の**埋め込みウィジェット**（HPは別管理、予約エンジンだけをウィジェット配信）。→ **不採用**。YAMADO は全HPを自社保有しており外部サイトへの埋め込みで得るものが乏しい一方、**クロスオリジン会員認証**（サードパーティCookie廃止の直撃）という最難関を抱え込むだけになる。

## 決定（Decision）

「**マーケティングHP ＋ 予約ポータル（リダイレクト）**」型を採用する（星野リゾート等の実運用と同型）。

1. **施設HPは独立サイトとして外部に維持する**（autumn-book で作り直さない）。基盤technology（WP／静的フレームワーク等）は問わない — 本質は「autumn-bookアプリが施設HPを配信しない」という責務分離であり、HP側の実装基盤の刷新はこの決定と矛盾しない（2026-07-02追記参照）。演出・SEO資産・URLはHP側の刷新であっても温存を前提とする。
2. **予約エンジン・会員機能・掲示板・ポータル地図検索を、単一の「YAMADOブランドポータル」**（`stay.yamado.co.jp`・最終名称は未確定）に統合する。SSR・全機能ファーストパーティ。
3. **予約エンジンはウィジェット化しない**。ポータルの一機能とする。
4. **施設HPの「予約」導線はブランドポータルへリダイレクト**（deep-link でハンドオフ）。

> これは再設計ではなく「**施設HPを再構築しないというスコープ判断**」。`book` スキーマ・RPC・会員・予約トランザクション・掲示板・客室電子インフォ・ポータル地図検索など、当初設計の本体は引き続き有効。

### 全体像（3層）

```
┌─ 施設HP層（既存・外部・WPのまま／別ドメイン・別管理）───────────┐
│  www.yamado.co.jp（西和賀HP）      oga.yamado.co.jp（男鹿HP）       │
│  ブランド/世界観。SEOは各HPが保持。「予約」= ポータルへリダイレクト   │
└───────────────────────┬───────────────────────────────────────┘
                        │ deep-link（facility/plan/日程をURLに載せる）
        ┌───────────────▼────────────────────────────────┐
        │  YAMADO ブランドポータル（単一SSRサイト・yamado.co.jp配下）   │
        │  予約エンジン｜会員・マイページ・ポイント｜掲示板｜地図空室検索  │
        │  ＝ 予約はウィジェットでなくポータルの一機能。全部ファーストパーティ │
        └────────────────────────────────────────────────┘
```

## 実装上の決めごと（成否を分ける6点）

この型は単純な分、**ハンドオフの作り込み**で成否が決まる。

1. **ディープリンクのハンドオフ契約を先に定義**（最大のコンバージョンリスク）。施設HPの「予約」は汎用ポータルTOPに落とさず、`…/booking?facility=oga&checkin=YYYY-MM-DD&nights=1&adults=2` のように**施設（理想は日程・プランまで）をURLに載せて**予約フローへ直接着地させる。各HPが叩く**安定エントリURLを契約として固定**する。
2. **プラン／客室／空室ページはポータル側**に置く。HPはブランド/世界観のみ。料金・在庫データを持つ側に mid-funnel を寄せる。当初設計 §7「プラン詳細＝SEO入口」は**ポータルドメインで**持つ。
3. **ポータルの予約画面を施設ごとに軽くテーマリング**。`book.brands.theme jsonb(配色・ロゴ)`（スキーマ実装済）＋施設名/ロゴ/ヒーローで「**oga の予約**」に見せ、ブランド断絶を緩和。→ 作り込んだ `OgaShell`/`YamadoShell` はここで流用でき無駄にならない。
4. **認証Cookieはポータルのホスト限定**（`stay.yamado.co.jp` host-only）。親 `.yamado.co.jp` に共有しない。HPは匿名ブローシャでセッション不要＝共有する理由がない。広域共有は過去の `.yamado.app` 共有Cookieで起きた **refresh競合・セッション増殖によるログイン不能インシデント**の再来リスク。host-only で当該クラスのバグを構造的に回避。
5. **SEOは2プロパティ運用**（掲示板の結論と整合）。GA4 クロスドメイン計測（当初設計 §9で決定済）でHP→ポータルのファネルを接続／canonical 棲み分け（HP＝「施設名・ブランド」、ポータル＝「施設名 予約／空室／プラン」）でカニバリ回避／掲示板UGCは横断1ハブとしてポータルドメインに集約。
6. **既存 `/[brand]/[facility]/` ルートは削除せず「ポータルの施設予約セクション」に転用**（客室・プラン・空室・アクセス）。フルマーケHPの野望だけ降ろす。

## 帰結（Consequences）

**得るもの**
- クロスオリジン会員認証という最難関を回避（ポータル内は全部ファーストパーティ）。
- 既存HPの演出・SEO資産・URLを温存。**予約エンジン置き換えとHP基盤刷新を分離**できる（予約導線はリンク差し替えのみで完結し、HP側の実装基盤変更に予約エンジンの開発サイクルを縛られない。2026-07-02追記参照）。
- ポータル内では会員＋予約＋掲示板の一体性を確保（当初設計の統合価値を維持）。
- 将来 YAMADO 以外へエンジンを外販したくなった場合でも、ポータルの予約フローを後からウィジェット化する道は残る（YAGNI として今は作らない）。

**失うもの / コスト**
- 一休型の「全ページ常時スティッキー検索で閲覧→予約が途切れない連続体験」は **HP↔ポータル境界で1回ジャンプ**が入る（ポータル内では維持）。これは「全HPをSvelte化しない」ことの対価。
- **2CMS運用**（HP＝独立CMS／プラン・予約・掲示板＝autumn admin）の軽い運用コスト。※2026-07-02追記: hp-yamado刷新ではお知らせを`book.news_posts`に一元化し、管理画面もautumn-book adminに統一する方針のため、この軽量化が進んでいる。

## 2026-07-02 追記：施設HP基盤の刷新（hp-yamado / hp-oga）

- 決定当時（2026-06-21）は「施設HPはWPのまま据え置く」が前提だったが、その後**施設HP自体をWordPressから軽量な静的フレームワーク（SvelteKit + Cloudflare Pages、hp-corporateの実装パターンを踏襲）へ刷新する**方針が別途決定された。
- 本ADRの決定1が指す「据え置く」は**「autumn-bookが施設HPを配信しない」という責務分離**が本質であり、HP側の実装基盤（WP or 静的フレームワーク）を問うものではない。したがってHP基盤の刷新はADR-0001と矛盾しない。
- HP刷新にあわせ、以下の**接続方針**を追加で決定：
  - hp-yamado / hp-oga の「お知らせ」機能は独自CMSを持たず、`book.news_posts`（既存・PROD適用済）を直接参照する（読み取りは匿名キーでRLS越しに公開記事のみ取得）。カテゴリ機能（`news_category`）はWP側で廃止し、移植しない。
  - お知らせの投稿・編集は**autumn-book既存の`/admin/news`に一本化**し、hp-yamado / hp-oga側に独自の管理画面は作らない。
  - 予約・会員ログイン・掲示板は本ADR決定どおりポータル（`stay.yamado.co.jp`）へのディープリンク／リンクアウトに統一し、施設HP側には実装しない（変更なし）。

## 旧設計（`autumn_book_design.md`）への影響

- **置換**：§0 結論サマリの「フロント＝全施設HP配信」ニュアンス、および §4.0 の「単一アプリが `reroute` で全施設HPを配信し、施設HPを Svelte シェルで再現する」方針。
- **存続**：§3（`book` スキーマ）／§5（anon境界・RPC・RLS）／§8（予約確定トランザクション）／§6（地図検索）／§15（掲示板・客室電子インフォ・ステップメール等）など本体はそのまま有効。
- `autumn_book_forum_design.md`（掲示板）・`autumn_book_inroom_design.md`（客室情報）は**ポータルに帰属**するため本ADRと整合（変更不要）。

## 次の作業（Fable 5 で詳細設計に展開）

- [ ] ディープリンク URL 契約の確定（パラメータ・正規化・施設未指定時のフォールバック）。
- [ ] canonical / キーワード棲み分けマップ（HP×ポータル）の明文化。
- [ ] 施設別テーマリング仕様（`book.brands.theme` のスキーマ確定と予約画面への適用範囲）。
- [ ] セッション／Cookie スコープ設計（host-only 化・OAuthコールバックのホスト）。
- [ ] `autumn_book_design.md` §4.0 のドメイン戦略を本ADRに合わせて改訂（ポータルホスト名の決定含む）。
