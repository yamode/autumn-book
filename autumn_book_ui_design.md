# autumn-book インターフェース設計書（v1）

> 本書は `autumn_book_design.md`（設計書v2）の **画面設計編**。データモデル・API・ドメイン戦略はv2を正とし、本書は各インターフェースの構成・レイアウト・状態・操作を定義する。
> 対象：顧客向け（施設HP・検索マップ・予約・会員）＋ 施設側（予約管理・プラン作成・メルマガ作成）。
> 実装スタック：SvelteKit 2 + Svelte 5 + Tailwind 4。共通コンポーネントは `packages/ui` に置く。

---

## 1. 画面一覧と対応 Phase

### 顧客向け（公開面）

| # | 画面 | ルート | Phase | 主データ源 |
|---|---|---|---|---|
| C-01 | ポータルトップ | `/` | P1 | book.v_facilities, brands |
| C-02 | 空室検索マップ | `/search` | P2 | RPC search_availability |
| C-03 | ブランドトップ | `/[brand]/` | P1 | brands, v_facilities |
| C-04 | 施設HP（フロント） | `/[brand]/[facility]/` | P1 | v_facilities, facility_photos, faqs |
| C-05 | 部屋タイプ詳細 | `/[brand]/[facility]/rooms/[room]` | P1 | v_room_types |
| C-06 | プラン一覧 | `/[brand]/[facility]/plans` | P2 | v_plans |
| C-07 | プラン詳細＋料金カレンダー | `/[brand]/[facility]/plans/[plan]` | P2 | v_plans, RPC get_plan_calendar / quote |
| C-08 | 予約：ゲスト情報入力 | `/booking/hold` | P3 | RPC create_hold |
| C-09 | 予約：決済 | `/booking/payment` | P4 | Stripe Elements |
| C-10 | 予約：完了 | `/booking/complete/[code]` | P3 | RPC confirm_booking |
| C-11 | ログイン／会員登録 | `/auth/login` `/auth/register` | P5 | Supabase Auth |
| C-12 | マイページトップ | `/account/` | P5 | RPC my_reservations, point_ledger |
| C-13 | 予約詳細・変更・キャンセル | `/account/reservations/[code]` | P5 | RPC my_reservations / cancel_booking |
| C-14 | ポイント・グレード | `/account/points` | P5 | point_ledger, member_ranks |
| C-15 | お気に入り | `/account/favorites` | P5 | favorites |
| C-16 | プロフィール・メール設定 | `/account/profile` | P5 | members |

### 施設側（管理面 `/admin` — 同一アプリ内・社内認証）

| # | 画面 | ルート | Phase | 主データ |
|---|---|---|---|---|
| A-01 | 管理ダッシュボード | `/admin` | P3 | bookings 集計, holds |
| A-02 | 予約管理（一覧・詳細） | `/admin/reservations` | P3 | booking.bookings, core.stays |
| A-03 | 施設コンテンツ編集 | `/admin/facility` | P1 | facility_contents, facility_photos |
| A-04 | 部屋コンテンツ編集 | `/admin/rooms` | P1 | room_type_contents |
| A-05 | プラン作成・編集（Markdown） | `/admin/plans` | P2 | plan_contents（＋booking.rate_plans 参照） |
| A-06 | メルマガ作成・配信 | `/admin/mail` | P7 | mail_campaigns, mail_sends |
| A-07 | ステップメール管理 | `/admin/sequences` | P7 | email_sequences, sequence_steps |
| A-08 | 会員管理 | `/admin/members` | P5 | members, point_ledger |
| A-09 | FAQ管理 | `/admin/faqs` | P1 | faqs |

- 管理面の認証は **rms と同じ社内 Auth（facility_admin / facility_staff ロール）**。顧客 Auth とはセッションを分離（管理面は `.yamado.app` 系の社内SSOではなく、本リポ内の authenticated + ロール判定。設計書 §5.3 の「facility_admin 以上が編集」に対応）。
- 管理面はインデックス禁止（`noindex` + Cloudflare Access 検討は P3 で判断）。

---

## 2. 共通UI基盤

### 2.1 レイアウト構造（顧客向け）

```
┌──────────────────────────────────────────────┐
│ ヘッダー: [ブランドロゴ] [宿を探す] [───検索バー───] [ログイン/会員名] │ ← sticky
├──────────────────────────────────────────────┤
│ （ページ本体）                                  │
├──────────────────────────────────────────────┤
│ フッター: 施設一覧 / 会社情報 / 特商法 / 約款 /     │
│          プライバシーポリシー / お問い合わせ        │
└──────────────────────────────────────────────┘
```

- **検索バー（全ページ共通・スティッキー）**：`[チェックイン日▼] [泊数▼] [大人2・子供0▼] [検索]`。値は URL クエリ（`?checkin&nights&adults&children`）と同期し、ページ遷移しても保持。変更時は遷移なしで料金・在庫を再取得（設計書 §7 の一休方式）。
- **ホスト別テーマ**：`reroute` で解決した brand の `theme` jsonb（配色・ロゴ）を CSS 変数に展開。ポータル（stay.*）は中立テーマ。
- **レスポンシブ**：モバイルファースト。ブレークポイント `md=768px / lg=1024px`。地図検索のみ専用モバイルレイアウト（§3.2）。

### 2.2 packages/ui の共通コンポーネント

| コンポーネント | 用途 | 主 props |
|---|---|---|
| `SearchBar` | 日付・人数の常時編集バー | checkin, nights, adults, children, onChange |
| `DateRangePicker` | カレンダー2ヶ月表示・連泊選択 | 選択可能範囲, 満室日 disabled |
| `GuestPicker` | 大人・子供のステッパー | 子供受付は未決（§14-11）のため子供行はフラグで隠せる |
| `PlanCard` | プラン一覧カード | photo, headline, tags, 総額/1名額, 残室, キャンセル文言 |
| `RoomCard` | 部屋タイプカード | photos, 定員, 広さ, amenities |
| `PriceCalendar` | 月間の日別最安値・◎○△× | get_plan_calendar の結果 |
| `PriceBreakdown` | 料金内訳（人数帯×泊・税・ポイント） | quote 結果 |
| `MapPanel` | MapLibre ラッパー・料金バッジピン | facilities[], onPinClick |
| `PhotoGallery` | 大判ギャラリー＋カテゴリタブ | photos[], categories |
| `HoldTimer` | 仮押さえ残り時間表示 | expires_at（残5分で警告色） |
| `PointBadge` | 「○○pt 利用可/付与予定」表示 | balance, earn |
| `RankBadge` | 会員グレード表示 | rank_code（standard/silver/gold） |
| `CancelPolicyNote` | 「6/20まで無料」動的文言 | cancellation_policy snapshot, checkin |
| `MarkdownView` | plan_contents 等の本文描画 | sanitize 済み HTML（許可タグ限定） |
| `Stepper` | 予約フロー進捗（①〜④） | current |

### 2.3 Markdown の扱い（共通仕様）

- 保存形式：**Markdown 原文を DB に保存**（`plan_contents.description` 等）。描画時に SSR で HTML 化。
- 許可記法：見出し（h2/h3）・強調・リスト・表・リンク・画像。**生 HTML は不許可**（XSS 防止のため sanitize で除去）。
- 画像は `![](URL)` で facility_photos にアップ済みの URL を参照（エディタから挿入。§4.5）。

---

## 3. 顧客向け画面詳細

### 3.1 C-01 ポータルトップ（`/`）

役割：全ブランド・全施設への入口。地図検索への導線が主役。

```
┌────────────────────────────────────────┐
│ ヒーロー：ブランド世界観の写真 + コピー        │
│   [日付・人数を選んで空室を探す →]  ←検索バー大型版 │
├────────────────────────────────────────┤
│ 地図プレビュー（全国・施設ピン / クリックで /search へ）│
├────────────────────────────────────────┤
│ 施設カード横並び：山人-yamado- / 山人-oga-     │
│   写真・キャッチコピー・参考最低料金「¥xx,xxx〜」 │
├────────────────────────────────────────┤
│ 会員制度の案内（ポイント還元・会員価格） [登録へ] │
└────────────────────────────────────────┘
```

- 日付未指定でも参考最低料金（直近30日の最安）を表示。SEO 入口を兼ねる（設計書 §6.2）。

### 3.2 C-02 空室検索マップ（`/search`）

設計書 §6.2 を画面仕様に落とす。

**デスクトップ（2ペイン）**

```
[検索バー：日付・泊数・人数・(絞り込み: 温泉/食事/料金帯)]
┌───────────────────────────┬─────────────────┐
│ MapLibre 地図               │ 施設カードリスト    │
│  ピン＝施設                  │ ┌─────────────┐ │
│  バッジ「¥23,100〜」         │ │写真│山人-yamado-│ │
│  満室＝灰色「満室」           │ │    │¥23,100〜  │ │
│  ホバーでカードをハイライト     │ │    │残り2室     │ │
│                             │ │ [プランを見る] │ │
│                             │ └─────────────┘ │
└───────────────────────────┴─────────────────┘
```

**モバイル**：地図全画面 + 下部に横スクロールのカードカルーセル（ボトムシート）。ピンタップでカードへスクロール同期。

状態と挙動：

| 状態 | 表示 |
|---|---|
| 日付未指定 | 全ピンに「参考 ¥xx,xxx〜」、カードに「日付を選ぶと空室と正確な料金が出ます」 |
| 検索中 | ピンバッジをスケルトン表示（地図は維持） |
| 該当施設の満室 | 灰色ピン「満室」＋カードに「キャンセル待ちは電話で」 |
| 残室 ≤2 | カードに「残り N 室」（実数のみ・設計書 §7） |
| RPC エラー | トースト「空室情報を取得できませんでした。再試行」＋施設HPリンクは生かす（フェイルソフト） |

- 検索条件変更 → `search_availability` 1回で全施設分更新（デバウンス 400ms）。
- カードの「プランを見る」→ `/[brand]/[facility]/plans?checkin=…`（条件引き継ぎ）。

### 3.3 C-04 施設HP（フロント・`/[brand]/[facility]/`）

旧WP（www / oga）を置き換える施設の顔。SSR + エッジキャッシュ。

```
┌────────────────────────────────────────┐
│ ヒーロー写真（フルワイド・スライド）            │
│  施設名 / キャッチコピー                      │
│  [空室を探す]（検索バーへフォーカス）           │
├────────────────────────────────────────┤
│ コンセプト（catch_copy + description）        │
├────────────────────────────────────────┤
│ フォトギャラリー（タブ: 客室/温泉/食事/外観/景色） │
├────────────────────────────────────────┤
│ 客室一覧（RoomCard 横並び → C-05 へ）         │
├────────────────────────────────────────┤
│ おすすめプラン（PlanCard 上位3件 → C-06 へ）    │
├────────────────────────────────────────┤
│ 空室カレンダー（月表示・日別最安値 → 日タップで    │
│  その日程のプラン一覧へ）                      │
├────────────────────────────────────────┤
│ アクセス（access jsonb を描画 + 地図 +          │
│  Googleマップ経路リンク + 送迎案内）            │
├────────────────────────────────────────┤
│ よくある質問（faqs 静的表示・カテゴリ別アコーディオン）│
├────────────────────────────────────────┤
│ 施設概要（チェックイン/アウト・設備・住所・電話）    │
└────────────────────────────────────────┘
  右下固定：チャットウィジェット（P1=FAQ検索 → P9=LLM）
```

- 構造化データ：schema.org `Hotel`（住所・geo・電話・チェックイン時刻）。OGP はヒーロー写真。
- ページ全体が「予約導線つきパンフレット」。予約系 CTA は「空室を探す」「このプランで予約」の2種に統一。

### 3.4 C-07 プラン詳細（`/plans/[plan]`）

```
┌────────────────────────────────────────┐
│ プラン写真ギャラリー │ headline / highlight_tags │
│                    │ 食事: 夕朝食付 / 支払: 現地  │
│                    │ CancelPolicyNote          │
├────────────────────────────────────────┤
│ プラン本文（MarkdownView — A-05 で作成した文章）│
├────────────────────────────────────────┤
│ PriceCalendar（月送り・日別最安値・◎○△×）      │
│  日付タップ → 検索バーに反映 → 下の見積が更新     │
├────────────────────────────────────────┤
│ 部屋タイプ選択（このプランで予約可能な部屋）       │
│  ┌ 部屋カード ─ 「2名1室 ¥46,200（1名 ¥23,100）」│
│  │  税込総額 / 残り2室 / [この部屋で予約する] ──┼─→ create_hold
├────────────────────────────────────────┤
│ キャンセルポリシー全文 / 注意事項               │
└────────────────────────────────────────┘
```

- 「この部屋で予約する」押下時に `quote` → `create_hold` を連続実行。満室競合時は「ただいま満室になりました」を表示しカレンダーを再取得。

### 3.5 予約フロー（C-08〜C-10）

Stepper：`① プラン選択 → ② お客様情報 → ③ お支払い → ④ 完了`（現地払いは③をスキップし3ステップ表示）。

**C-08 ゲスト情報（`/booking/hold`）**

```
[HoldTimer: お部屋を確保しています 残り 18:42]
┌─ 予約内容サマリ（右カラム / モバイルは折りたたみ）─┐
│ 施設・部屋・プラン・日程・人数・PriceBreakdown      │
│ ポイント利用 [____pt 使う]（会員のみ・即時利用）     │
└─────────────────────────────────────┘
未ログイン時の分岐（一休方式・先に進める）:
  [会員ログインして入力を省く] [ゲストのまま続ける]
入力フォーム：
  氏名* / カナ* / 電話* / メール*（確認用再入力なし・形式検証のみ）
  到着予定時刻（select）/ 送迎希望（shuttle.available の施設のみ表示）
  連絡事項（任意・textarea）
  □ 会員登録してポイントを受け取る（ゲスト時のみ。チェックで登録フローへ合流）
[③ お支払いへ進む / 現地払いプランは「予約を確定する」]
```

- バリデーションはインライン即時（blur 時）。エラーで送信ボタンは無効化しない（押下時にエラー位置へスクロール）。
- タイマー失効時：モーダル「確保時間が過ぎました」→ 同条件で再 hold を試行 → 満室なら日程変更を促す。

**C-09 決済（`/booking/payment`）** — 事前カード払いプランのみ

- Stripe Payment Element（カードのみで開始）。金額・内訳を再掲。3DS は Stripe に委譲。
- 失敗時：エラー文言（カード会社の理由は出さず汎用文言）＋再試行。hold は維持。

**C-10 完了（`/booking/complete/[code]`）**

```
✔ ご予約が確定しました  予約番号 YB-2026-000123
  確認メールを送信しました（{email}）
  [カレンダーに追加 (ics)] [マイページで確認]
  ─ ゲスト予約の場合 ─
  「会員登録すると本予約に ○○pt が付きます」[30秒で登録]
  ─ クロスセル枠（P6 以降） ─
  「貸切風呂のご予約はこちら」
```

### 3.6 C-11 認証（登録・ログイン）

- **ログイン**：メール+パスワード / マジックリンク / Google / LINE（導入決定後）。1画面に縦並び。
- **会員登録**：メール・パスワード・氏名・カナ・電話・□メルマガ受信（is_mail_opt_in・既定OFF=オプトイン記録）。登録完了時に guest_identities 名寄せが裏で走る（UI には出さない）。
- 予約フロー内から登録した場合、入力済みのゲスト情報をプリフィルし、完了後フローに復帰する。

### 3.7 マイページ（C-12〜C-16）

共通レイアウト：左ナビ（モバイルはタブ）`予約 / ポイント / お気に入り / プロフィール`。

**C-12 トップ**：次回の宿泊カード（日程・施設・地図・「予約内容を見る」）＋ ポイント残高 ＋ RankBadge。

**C-13 予約詳細**

```
予約番号 / ステータス（予約済・キャンセル済・宿泊済）
施設・部屋・プラン・日程・人数・料金内訳・支払状況
CancelPolicyNote（現時点のキャンセル料を動的表示: 「本日キャンセル: ¥9,240 (20%)」）
[予約をキャンセルする] → 確認モーダル（キャンセル料・返金額・返金方法を明示）→ cancel_booking
[日程を変更する]（P10。それまでは「変更はお電話で」+ 施設電話番号）
─ オプション追加枠（P6）: 貸切風呂・ケーキ等 ─
```

**C-14 ポイント・グレード**

```
┌ 残高: 1,240 pt（うち 6/30 失効予定: 200 pt）┐
│ RankBadge: GOLD ─ 還元率 3%               │
│ 次のランクまで: あと2泊（進捗バー）            │ ← ランク条件は member_ranks 定義から動的描画
└──────────────────────────────────┘
履歴テーブル（point_ledger）: 日付 / 内容(reason) / +付与 −利用 / 失効予定日
```

**C-16 プロフィール**：氏名・カナ・電話の編集（core.guests へ反映）／メール変更（Auth 再確認フロー）／メルマガ ON/OFF／退会（論理削除・予約履歴は保持する旨を明示）。

---

## 4. 施設側管理画面（`/admin`）

### 4.1 共通シェル・認可

```
┌ サイドナビ ──────┬──────────────────────┐
│ 📊 ダッシュボード   │  ヘッダー: [施設切替▼] [ユーザー] │
│ 📅 予約管理        │                              │
│ 🏠 施設ページ編集   │  （コンテンツ）                 │
│ 🛏 部屋編集        │                              │
│ 📝 プラン          │                              │
│ ✉ メルマガ        │                              │
│ 🔁 ステップメール   │                              │
│ 👤 会員           │                              │
│ ❓ FAQ            │                              │
└─────────────────┴──────────────────────┘
```

- **施設切替**：ヘッダーのセレクタで対象施設を切替（superadmin は全施設、facility_admin は担当施設のみ）。全画面がこのコンテキストに従う。
- ロール：`facility_staff`=予約閲覧のみ／`facility_admin`=コンテンツ・プラン・メルマガ編集＋予約操作／`superadmin`=会員・ポイント調整含む全権。
- DEBUG パネル（全社方針）を `/admin` にも実装。

### 4.2 A-01 ダッシュボード

| ウィジェット | 内容 |
|---|---|
| 本日の動き | 本日のチェックイン件数 / 新規直販予約 / キャンセル |
| 直近30日 | 直販予約件数・金額の推移（簡易棒グラフ） |
| アクティブ hold | 現在仮押さえ中の件数（≒今まさに予約しようとしている人数） |
| アラート | SC 在庫通知の失敗（sync_logs）・決済不一致・メール送信失敗 |

### 4.3 A-02 予約管理

**一覧**

```
フィルタ: [期間(チェックイン基準)▼] [ステータス▼] [チャネル: 直販のみ/全て] [検索: 氏名・予約番号]
┌──────┬────────┬──────┬────┬──────┬────────┬──────┐
│予約番号 │ ゲスト    │チェックイン│泊数 │部屋    │金額/支払   │ステータス │
│YB-…123│ 山田 太郎 │ 6/20    │ 2  │和洋室A │¥46,200 済 │ 予約済   │
└──────┴────────┴──────┴────┴──────┴────────┴──────┘
表示は booking.bookings（channel='autumn_booking' を既定フィルタ）。OTA 予約も閲覧のみ可（運用の全体像把握用）。
```

**詳細（ドロワー or `/admin/reservations/[id]`）**

- 予約内容全項目＋ゲスト連絡先＋連絡事項・送迎希望＋ポイント利用額＋支払状況（Stripe PaymentIntent へのリンク）。
- 操作（facility_admin）：
  - **キャンセル（施設都合）**：キャンセル料 0 で cancel_booking を実行（理由必須・監査ログ）。
  - **キャンセル（ゲスト依頼の電話代行）**：通常のキャンセル料計算で実行。実行前に金額をプレビュー表示。
  - メール再送（確定メール）。
- **運用境界**：部屋割り・チェックイン操作・現場帳票は autumn-pms の領分。本画面は「直販予約の受付状況の確認と例外対応」に限定し、PMS 画面への相互リンクを置く（二重実装しない・設計書 §13）。

### 4.4 A-03 施設コンテンツ編集 / A-04 部屋編集

- フォーム編集：catch_copy / description（Markdown）/ チェックイン・アウト時刻 / amenities（チェックボックス群）/ access（構造化フォーム：車・電車・飛行機の行追加 UI、送迎・駐車場）。
- **写真管理**：ドラッグ&ドロップでアップロード（Cloudflare Images or Supabase Storage → URL を facility_photos へ）。カテゴリ（外観/客室/風呂/食事/景色）タグ付け・並べ替え（ドラッグ）・キャプション。
- **公開制御**：`is_published` トグル＋「下書きプレビュー」ボタン（プレビュー用署名付き URL で未公開状態を実画面確認）。
- 保存は明示ボタン（自動保存しない）。保存時に published_at 更新。

### 4.5 A-05 プラン作成・編集（Markdown エディタ）

プランは2層構造（設計書 §3.1）を画面でそのまま見せる：

```
┌ Step 1: 料金プランを選ぶ ──────────────────────┐
│ booking.rate_plans の一覧（rms で作成済みのもの）      │
│  [二食付スタンダード] 食事:夕朝 / 支払:現地 / 直販公開:✔ │
│  ※料金・在庫・キャンセル規定の変更は rms で行う（リンク） │
└────────────────────────────────────────┘
┌ Step 2: 見せ方をつくる（plan_contents） ─────────┐
│ headline  [一文キャッチ_________________________] │
│ highlight_tags  [露天風呂付×] [記念日×] [+追加▼]    │
│ 写真  [ギャラリーから選択 / アップロード]（複数・並べ替え）│
│ ┌─ Markdown エディタ ────┬─ ライブプレビュー ───┐ │
│ │ ## お料理               │ （PlanCard・詳細画面と │ │
│ │ 地元の山の幸を…          │  同一スタイルで描画）   │ │
│ │ ![](写真挿入ボタンでURL)  │                     │ │
│ │ ツールバー: 見出し/太字/   │                     │ │
│ │  リスト/表/写真挿入        │                     │ │
│ └───────────────────┴───────────────────┘ │
│ sort_order [3]   is_published [下書き ◯ 公開]      │
│ [プレビュー（顧客画面で確認）] [保存]                  │
└────────────────────────────────────────┘
```

- エディタ：textarea ベース＋ツールバー（記法挿入）＋右ペインにリアルタイムプレビュー（顧客側 MarkdownView と同一コンポーネント＝**見たまま一致を保証**）。モバイル管理時はタブ切替（編集/プレビュー）。
- 「写真挿入」はアップロード済み写真のピッカーを開き、選択で `![キャプション](URL)` をカーソル位置へ挿入。
- rate_plan 未選択では保存不可。1 rate_plan : 1 plan_contents（既に存在すれば編集モードで開く）。
- 一覧画面：プランカードのサムネイル一覧（公開/下書きバッジ・直販公開フラグが rms 側で OFF のものは「rms 非公開」警告表示）。

### 4.6 A-06 メルマガ作成・配信（P7）

3ステップウィザード＋一覧。

```
一覧: [タイトル / セグメント / 状態(draft/scheduled/sending/sent) / 開封率 / 操作]

ステップ1: 本文作成
  件名 [_______________________]
  本文: Markdown エディタ＋プレビュー（A-05 と同じ部品。メール用テンプレートに流し込み）
  差込変数: {氏名} {ランク} {ポイント残高}（ツールバーから挿入）
  ※フッターの配信停止リンク・送信者表示は自動付与（特定電子メール法・編集不可）

ステップ2: 宛先（セグメント）
  □ メルマガ受信 ON のみ（固定・外せない）
  ランク       [すべて / standard / silver / gold]（複数選択）
  宿泊履歴     [施設▼] [最終宿泊日が ◯ヶ月以内 / 以上前] [宿泊回数 ≥ n]
  お気に入り    [施設▼ を登録している]
  → 条件は segment jsonb に保存。[対象者を数える] → 「現在 312 名」
  [テスト送信（自分のメール宛）]

ステップ3: 配信
  ◯ 今すぐ配信  ◯ 日時指定 [2026-06-15 10:00]
  確認画面（件名・対象者数・プレビュー）→ [配信を確定する]（二段確認）

配信後: 開封率・送信成功/失敗数（mail_sends 集計）を一覧と詳細に表示
```

- 送信中（sending）は編集不可。mail_sends の UNIQUE 制約で二重送信を構造的に防止（設計書 §15.5）。

### 4.7 A-07 ステップメール管理（P7）

- シーケンス一覧（施設別・is_active トグル）。
- ステップ編集：タイミング（`チェックイン7日前` 等を offset_days で表現・プルダウン）＋送信時刻＋テンプレート本文（Markdown・差込変数：氏名/日程/部屋/プラン/送迎有無）＋[テスト送信]。
- 送信ログビュー：直近の email_sends（予約番号・ステップ・成否）。失敗はダッシュボードのアラートにも出す。

### 4.8 A-08 会員管理

- 一覧：member_code / 氏名 / ランク / ポイント残高 / 登録日 / メルマガ可否。検索（氏名・メール）。
- 詳細：予約履歴（guest_id 連結）・ポイント台帳・**手動ポイント調整**（superadmin のみ。delta と理由必須 → point_ledger に記帳）・ランク手動変更（理由必須）。
- 個人情報表示はロールで制御（facility_staff は連絡先マスク表示）。

### 4.9 A-09 FAQ 管理

- カテゴリ別一覧・並べ替え・question/answer（answer は Markdown）・is_published。チャットボット（§15.1）の知識源を兼ねる旨を画面に注記。

---

## 5. ポイント・グレード表示ルール（横断仕様）

| 場面 | 表示 |
|---|---|
| プラン詳細・見積 | 「○○pt 付与予定」（rank 還元率 × 税抜額。会員のみ。未ログイン時は「会員なら○○pt」で登録誘導） |
| 予約フロー C-08 | 残高内で利用 pt を入力（即時利用：本予約の付与予定 pt も原資に含む — 設計書 §3.2）。利用後の支払額を即時再計算 |
| キャンセル時 | 利用 pt は全額返還・付与予定 pt は取消（point_ledger 逆仕訳）。モーダルに明記 |
| 失効 | 30日以内に失効する pt をマイページと（オプトイン者には）メールで予告 |
| ランク昇格 | 判定は夜間バッチ（条件は member_ranks 定義）。昇格時にマイページバナー＋メール |

※ 還元率・原資・昇格条件の数値は未決（設計書 §14-10）。UI は member_ranks テーブル駆動とし、数値確定後にデータ投入だけで反映できる構造にする。

---

## 6. 状態・エラー設計（共通）

| ケース | 挙動 |
|---|---|
| Supabase 障害 | 施設HP はエッジキャッシュで閲覧継続。検索・予約は「ただいま予約を受け付けられません。お電話（施設番号）へ」のフェイルソフト表示 |
| hold 競合（version 不一致） | 1回自動リトライ → 失敗で「満室になりました」＋カレンダー再取得 |
| 決済成功・confirm 失敗 | 完了画面の代わりに「予約処理中」画面＋自動リトライ。最終失敗時は Webhook 側で検知し返金＋アラート（A-01） |
| セッション切れ（マイページ） | ログイン画面へ。復帰後に元 URL へ戻す |
| 二重送信 | 全確定系ボタンは押下で即 disabled + idempotency key（hold_id / campaign_id） |

---

## 7. 本書で確定する実装上の追加事項

設計書v2 に対する増分（migration・RPC への影響）：

1. `book.plan_contents.description` は **Markdown 原文**を保存（v2 では型未指定だった点の確定）。facility_contents.description / faqs.answer / mail_campaigns・sequence_steps の本文も同様。
2. 管理画面用に書き込み系の社内 RPC ではなく、**authenticated + facility_admin RLS の直接 UPDATE/INSERT** を許可（contents 系テーブルのみ。設計書 §5.3 の表と整合）。
3. 施設都合キャンセル用に `cancel_booking` へ `waive_fee boolean`（社内ロールのみ true 可）と `reason` を追加。
4. 手動ポイント調整は新 RPC `book.adjust_points(member_id, delta, reason)`（superadmin のみ）。
5. メルマガの「対象者を数える」用 RPC `book.count_segment(segment jsonb)`。
6. 監査：管理画面の破壊的操作（キャンセル・ポイント調整・ランク変更）は `book.admin_audit_logs` に記録（新規テーブル・P3 で追加）。

## 8. 未決事項（本書で新たに発生・要ユーザー判断）

1. **管理画面の置き場所**：本書は autumn-book 内 `/admin` 前提で設計（rms 同居案は不採用提案）。承認要。
2. **写真ストレージ**：Cloudflare Images（変換・配信込み・有償）か Supabase Storage + 自前リサイズか。
3. **管理面の追加保護**：Cloudflare Access（社内メール限定）を被せるか。
4. **OTA 予約の管理画面表示**：閲覧のみ表示する（本書案）か、直販のみに絞るか。
5. **プラン作成の範囲**：rate_plans（料金・規定）の新規作成まで book 管理画面に取り込むか、rms に残すか（本書は rms 残置案）。
